services:
  - type: web
    name: mindful-app
    env: docker           # 👈 tells Render to build from your Dockerfile
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: REALTIME_MODEL
        value: gpt-4o-realtime-preview-2024-12-17
    healthCheckPath: /api/health
